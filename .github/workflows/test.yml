name: PowerShell Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup PowerShell Profile
      shell: pwsh
      run: |
        $ProfileDir = Split-Path -Parent $PROFILE
        if (-not (Test-Path $ProfileDir)) {
          New-Item -ItemType Directory -Force -Path $ProfileDir | Out-Null
        }
        Copy-Item -Path .\Microsoft.PowerShell_profile.ps1 -Destination $PROFILE -Force
        Copy-Item -Path .\modules -Destination $ProfileDir -Recurse -Force

    - name: Run Unit Tests
      shell: pwsh
      run: |
        $ErrorActionPreference = 'Stop'
        ./tests/Microsoft.PowerShell_profile.Tests_diff.ps1 -Verbose
